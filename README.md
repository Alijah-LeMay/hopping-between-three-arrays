# hopping-between-three-arrays
Numeric game to hop between three arrays. From A to B, then B to A, then A to C, then C to A. detect max variables within each, and use the value from the previous array as the index in the next.
